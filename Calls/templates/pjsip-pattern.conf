[general] 
disable_multi_domain=on
transport = udp 

[global] 
type = global
endpoint_identifier_order=header,username,ip,anonymous
user_agent = <USER-AGENT>

[transport-udp]
type = transport
protocol = udp
bind=0.0.0.0:<SIP_PORT_MODULE>

